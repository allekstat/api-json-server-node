const HOST_SCHEME = 'http';
const HOST_IP = '127.0.0.1';
const HOST_PORT = '3000';
const HOST_NAME = `${HOST_SCHEME}://${HOST_IP}:${HOST_PORT}`;
const ROUTE = 'users';
const ENDPOINT = `${HOST_NAME}/${ROUTE}`;
export { ENDPOINT };
